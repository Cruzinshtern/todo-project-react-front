import CreateTodo from '../features/todo/CreateTodo';

export default function TodoDesignerPage() {
  return (
    <div className="flex content-center h-screen">
      <CreateTodo />
    </div>
  );
}
